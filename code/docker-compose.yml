version: '3'

services:
  backend:
    build: ./server
    ports:
      - "8000:8000"
    env_file:
    - ./server/.env

  frontend:
    build: ./client
    ports:
      - "8080:5173"
    env_file:
    - ./client/.env
     
